#include <iostream>
#include <map>
#include <string>
using namespace std;

class map<string K, int V>{
  class Node{
    public:
   string  key;
    int value;

    Node(string key, int value){
        this->key = key;
        this->value = value;
    }
  }
};